#include <iostream>
#include <vector>

using namespace std;

template <typename T>
class MessAround {

private:

public:
	MessAround(T a) {
		vector<T> vect;
		vect.push_back(a);
		vect.back().
	}
};

int main() {



	cin.get();
	cin.get();
	return 0;
}